<Window x:Class="Names.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Names"
        mc:Ignorable="d"
        Title="Names" Height="400" Width="450" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Content="Names" />
        <ListBox x:Name="listOfNames" Grid.Row="1" />

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
            <TextBox x:Name="nameBox" Padding="3" />
            <Button Content="Add" Margin="0, 5, 0, 0" Click="AddNameToListBox" />
            <Button Content="Clear Box" Margin="0, 5" Click="ClearBox" />
            <Button Content="Remove Name" Click="RemoveName" />
        </StackPanel>
    </Grid>
</Window>
